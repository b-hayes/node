//Plugins can hook into the entire lifecycle of the build process
// im not going to do plugins just yet but the basic idea is to
// install one from https://webpack.js.org/plugins/
//and then use them in the config like so..

    // plugins: [
    //     new BundleAnalyzerPlugin()
    // ]

